package twg2.collections.util.test;


public class UnitTest {


	public static void main(String[] args) throws Throwable {
		/*List<Integer> listInt = new ArrayList<Integer>();
		UniqueIdList<Object> unique = new UniqueIdList<Object>();
		Object[] values = new Object[] {2, 4, 8, 16, 32, 64, 128, 256};
		for(int i = 0; i < values.length; i++) {
			unique.createId(values[i]);
			if(i == 1) {
				System.out.print("Removed: " + unique.toString());
				int value = unique.deleteValue(values[1]);
				System.out.println(" to " + unique.toString());
				System.out.println("Deleted value " + values[1] + " id: " + value);
			}
			if(i == 3) {
				System.out.print("Removed: " + unique.toString());
				Object value = unique.deleteId(2);
				System.out.println(" to " + unique.toString());
				System.out.println("Deleted id 2 value: " + value);
			}
			if(i == 5) {
				System.out.print("Removed: " + unique.toString());
				Object value = unique.deleteId(3);
				System.out.println(" to " + unique.toString());
				System.out.println("Deleted id 3 value: " + value);
			}
			System.out.println(unique.toString());
		}
		System.out.println("Final: " + unique.toString());
		unique.deleteId(0);
		unique.deleteValue(values[2]);
		unique.deleteValue(values[4]);
		unique.deleteValue(values[6]);
		unique.deleteValue(values[7]);
		System.out.println("Unique List: " + unique.toString());
		unique.createId(3);
		unique.clear();
		System.out.println("Unique Clea: " + unique.toString());
		unique.createId(11);
		System.out.println("Unique Item: " + unique.toString());

		int[] items = new int[] {5, 9, 12, 3, 4, 3, 2, 1};
		int size = items.length;

		UniqueIdGenerator gen = new UniqueIdGenerator();
		int[] ids = new int[18];
		size = ids.length;
		for(int i = 0; i < size; i++) {
			ids[i] = gen.createId();
		}
		gen.deleteId(ids[3]);
		ids[3] = -1;
		System.out.println(Arrays.toString(ids));
		System.out.println(gen);
		gen.deleteId(ids[5]);
		ids[5] = -1;
		System.out.println(Arrays.toString(ids));
		System.out.println(gen);
		ids[3] = gen.createId();
		System.out.println(Arrays.toString(ids));
		System.out.println(gen);
		ids[5] = gen.createId();
		System.out.println(Arrays.toString(ids));
		System.out.println(gen);
		 */
	}

}
